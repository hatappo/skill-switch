{
  "name": "skill-switch",
  "version": "0.1.0",
  "description": "TUI for switching user-level Agent Skills across Codex, Claude Code, and Cursor.",
  "bin": {
    "skill-switch": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "start": "node src/cli.ts",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "syntax": "node --check src/cli.ts && node --check src/core.ts && node --check src/tui.ts && node --check tests/core.test.ts",
    "test": "node --test tests/*.test.ts",
    "check": "pnpm lint && pnpm format:check && pnpm syntax && pnpm test"
  },
  "devDependencies": {
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0"
  },
  "engines": {
    "node": ">=26"
  },
  "packageManager": "pnpm@11.3.0"
}
